import { Page } from '@playwright/test';

//navigation functionality centred in this base page class

export class BasePage {
  protected page: Page;

  constructor(page: Page) {
    this.page = page;
  }

  async navigateTo(url: string): Promise<void> {
    await this.page.goto(url);
  }
}
